import React from "react";

const Footer = () => {
    return (
        <footer>
            <p>© {new Date().getFullYear()} Carella Connect. All rights reserved.</p>
        </footer>

    );
};

export default Footer;